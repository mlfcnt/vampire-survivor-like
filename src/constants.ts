export const canvasSize = {
  width: 500,
  height: 500, // Adjust as needed
};

export const characterSize = {
  width: 20,
  height: 20,
};

export const characterColor = 'blue';

export const playerSpeed = 20;

export const projectileSpeed = 1;
export const projectileFireRateMs = 500;
export const projectileSize = {
  width: 7,
  height: 7,
};

export const projectileColor = 'red';

export const enemiesSpawnRate = 1000;
export const enemyColor = 'purple';
export const enemySize = {
  width: 12,
  height: 12,
};
